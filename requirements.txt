requests>=2.31
beautifulsoup4>=4.12
openpyxl>=3.1
python-docx>=1.1
rich>=13.0
streamlit>=1.35
# starlette 1.4.0 ajoute thread_minimum_size (obligatoire) a GZipResponder.__init__,
# que streamlit ne passe pas -> TypeError au demarrage. A relever quand streamlit corrigera.
# Borne basse : DEFAULT_EXCLUDED_CONTENT_TYPES / IdentityResponder n'existent qu'a partir de 0.46.
starlette>=0.46,<1.4
playwright>=1.40
camoufox>=0.4
folium>=0.17
streamlit-folium>=0.20
plotly>=5.0
